import { FieldItem } from './field-item';

export class TextboxItem extends FieldItem<string> {
  controlType = 'textbox';
  type: string;

  constructor(options: {} = {}) {
    super(options);
    this.type = options['type'] || '';
  }
}